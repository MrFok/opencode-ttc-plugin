{
  "name": "token-company-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OpenAI-compatible proxy for Cursor with compression-ready middleware",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js",
    "smoke:e2e": "node scripts/smoke-e2e.mjs",
    "load:conformance": "node scripts/load-conformance.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
